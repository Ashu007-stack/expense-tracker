import {ShieldCheck,Mail,Phone,LogOut,UserRound} from "lucide-react";
import {useNavigate} from "react-router-dom";
import useAuth from "../context/useAuth";
import "./FinancePages.css";
export default function Profile(){const {user,logout}=useAuth();const navigate=useNavigate();const initials=(user?.full_name||"U").split(" ").map(x=>x[0]).slice(0,2).join("").toUpperCase();function signOut(){logout();navigate("/login",{replace:true})}return <div className="finance-page"><div className="page-heading"><div><p className="eyebrow">ACCOUNT</p><h1>Profile</h1><p>Manage your account information and session.</p></div></div><div className="profile-grid"><section className="profile-hero panel"><div className="profile-avatar">{initials}</div><div><h2>{user?.full_name}</h2><p>{user?.email}</p></div></section><section className="panel"><p className="eyebrow">ACCOUNT DETAILS</p><div className="detail-row"><UserRound size={18}/><div><span>Full name</span><strong>{user?.full_name}</strong></div></div><div className="detail-row"><Mail size={18}/><div><span>Email</span><strong>{user?.email}</strong></div></div><div className="detail-row"><Phone size={18}/><div><span>Mobile number</span><strong>{user?.mobile_number||"Not provided"}</strong></div></div><div className="security-box"><ShieldCheck size={20}/><div><strong>Authenticated session</strong><p>Your dashboard requests use your current Bearer token.</p></div></div><button className="danger-btn" onClick={signOut}><LogOut size={17}/> Sign out</button></section></div></div>}
